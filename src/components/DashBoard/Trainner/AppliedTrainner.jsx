
import React from 'react';

const AppliedTrainner = ({ appliedUserId }) => {
  const confirmTrainer = async () => {
   
  };

  return (
    <button className='btn btn-primary'  onClick={confirmTrainer}>
      Confirm as Trainer
    </button>
  );
};

export default AppliedTrainner;
